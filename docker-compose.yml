services:
  ai:
    build: ./ai
    container_name: loja-aasiam-ai
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - ./ai/.env
    volumes:
      - ./ai/docs:/app/docs
      - ai_chroma:/app/chroma_db

volumes:
  ai_chroma:
